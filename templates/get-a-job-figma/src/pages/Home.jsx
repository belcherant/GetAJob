import React from 'react'

export default function Home(){
  return (
    <div className="container">
      <h1>GetAJob — Figma conversion (placeholder)</h1>
      <p>This template contains a React + Vite scaffold with plain CSS and routing. I will convert each Figma page into a dedicated React component and replace these placeholders once I have access to the Figma file content (or confirm it is viewable by link).</p>

      <div style={{marginTop:24}} className="placeholder">
        <strong>Placeholder</strong>
        <div style={{marginTop:8}}>Figma pages will be converted into components here.</div>
      </div>
    </div>
  )
}